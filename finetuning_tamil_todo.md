# Qwen3-ASR 0.6B Tamil Fine-tuning To-Do

## Goal

Fine-tune local `Qwen3-ASR-0.6B` on the local DeepDML IISC MILE Tamil ASR dataset, save processed training data under `/mnt/weka/aisg/speech_spoke/donghang/data/deepdml-iisc-mile-tamil-asr`, run training through Slurm only on the allowed nodes, then run inference on `/mnt/weka/aisg/speech_spoke/donghang/data/fleur/tamil/test.jsonl` and report WER/CER.

## To-Do List

| ID | Task | Validation Plan | Passing Result | Status |
| --- | --- | --- | --- | --- |
| T1 | Inspect local model, source dataset, FLEURS test set, and Slurm example. | Confirm paths exist; identify model snapshot path; inspect dataset fields/splits; inspect test JSONL fields; inspect sbatch resource style. | Paths and schemas are documented below with no missing critical input. | Complete |
| T2 | Convert DeepDML Tamil ASR data to training JSONL. | Generate JSONL entries with `audio`, `text`, and optional `prompt`; verify every audio path exists and at least a sample can be loaded. | Processed directory contains train JSONL and audio references accepted by `qwen3_asr_sft.py`. | Complete |
| T3 | Create Slurm fine-tuning script for Qwen3-ASR-0.6B. | Validate script uses `sbatch`, allowed `#SBATCH --nodelist`, local model snapshot, processed train JSONL, and repo fine-tune entrypoint. | Script is shell-syntax valid and ready to submit. | Complete |
| T4 | Submit fine-tuning job and monitor logs. | Submit with `sbatch`; inspect stdout/stderr; verify checkpoints under output dir. | At least one inferable checkpoint exists with copied HF config/tokenizer files. | Complete |
| T5 | Create and run FLEURS Tamil inference/evaluation. | Build script to load checkpoint, transcribe test audio, normalize text, compute WER/CER; run via Slurm on allowed nodes. | Evaluation output includes prediction JSONL plus WER and CER summary. | Complete |
| T5b | Run pretrained Qwen3-ASR-0.6B FLEURS Tamil baseline. | Submit a Slurm job that loads the original local 0.6B snapshot and writes metrics to a separate output directory. | Baseline output contains `predictions.jsonl` and `metrics.json` with WER/CER. | Complete |
| T6 | Record final commands, artifacts, and metrics. | Update this file with paths, job ids, checkpoint used, and metric values. | File contains reproducible handoff and all tasks marked complete. | Pending |

## Progress Notes

- Initialized task tracker.
- T1 complete. Model snapshot path is `/mnt/weka/aisg/speech_spoke/donghang/hf_cache/hub/models--Qwen--Qwen3-ASR-0.6B/snapshots/5eb144179a02acc5e5ba31e748d22b0cf3e303b0`. DeepDML snapshot path is `/mnt/weka/aisg/speech_spoke/donghang/hf_cache/hub/datasets--deepdml--iisc-mile-tamil-asr/snapshots/32f434a8e3fe6705304964fffd90dab3383ee6d1`, with `train` 77,314 rows and `test` 12,087 rows. Parquet fields are `audio.bytes`, `audio.path`, `transcription`, `id`, and `duration`. FLEURS Tamil test has 591 rows with `audio_path`, `text`, `prompt_path`, and `prompt_text`; all sampled audio exists at 16 kHz mono WAV. Slurm scripts must include `#SBATCH --nodelist=smc-pod-3,smc-pod-4,smc-pod-5,smc-pod-8,smc-pod-9,smc-pod-10`. The active Python env is `/mnt/weka/aisg/speech_spoke/donghang/envs/qwen3asr`; installed missing dependencies with `uv pip install datasets pyarrow jiwer`, which adjusted `fsspec` to `2026.6.0`.
- T2 started with `scripts/prepare_deepdml_tamil_asr.py`.
- T2 complete. Generated `/mnt/weka/aisg/speech_spoke/donghang/data/deepdml-iisc-mile-tamil-asr/train.jsonl` with 77,314 rows and `eval.jsonl` with 12,087 rows. Wrote 89,401 WAV files under `audio/`; target directory size is about 13G. Validation loaded sampled WAVs with `soundfile`, confirmed 16 kHz mono audio, existing paths, and `language Tamil<asr_text>` targets.
- T3 complete. Added `scripts/train_qwen3_asr_0_6b_tamil_sft.sh`, `scripts/eval_qwen3_asr_fleurs_tamil.py`, and `scripts/eval_qwen3_asr_fleurs_tamil.sh`. Verified with `bash -n` and `python -m py_compile`. Both Slurm wrappers use the required node allowlist and activate `/mnt/weka/aisg/speech_spoke/donghang/envs/qwen3asr`.
- T4 update. Initial training job `168739` was cancelled because `qwen3asr` had `torch 2.13.0+cu130`, which could not use the CUDA 12.4 driver on `smc-pod-4`. Reinstalled `torch==2.6.0+cu124` in `/mnt/weka/aisg/speech_spoke/donghang/envs/qwen3asr` with `uv pip install --index-url https://download.pytorch.org/whl/cu124 torch==2.6.0`. Slurm CUDA check job `168746` passed: CUDA available on NVIDIA H200. Restarted training as job `168749`; it is running on `smc-pod-4` and entered the 2417-step training loop.
- T4 update. `checkpoint-200` exists and is inferable: it contains model weights plus copied HF config/tokenizer/chat-template files. `trainer_state.json` reports step 200 of 2417 and `eval_loss=0.09817168116569519`. Full epoch training is still running.
- T5 script-ready update. `scripts/eval_qwen3_asr_fleurs_tamil.py` now supports full inference, conservative Tamil text normalization, corpus WER/CER, per-sample WER/CER, `--dry_run` manifest validation, and `--predictions_jsonl` metrics-only recomputation. `scripts/eval_qwen3_asr_fleurs_tamil.sh` runs through Slurm on the required node allowlist, activates `/mnt/weka/aisg/speech_spoke/donghang/envs/qwen3asr`, supports `MODEL_PATH`, `TRAIN_OUTPUT_DIR`, or `CHECKPOINT_STEP`, and defaults to the latest `checkpoint-*`. Validation passed: `python -m py_compile`, `bash -n`, `--dry_run --limit 5` with zero missing audio and zero empty references, and metrics-only test with WER/CER both 0.0.
- T5b update. Added `scripts/eval_qwen3_asr_0_6b_pretrained_fleurs_tamil.sh` for the pre-finetune baseline using `/mnt/weka/aisg/speech_spoke/donghang/hf_cache/hub/models--Qwen--Qwen3-ASR-0.6B/snapshots/5eb144179a02acc5e5ba31e748d22b0cf3e303b0`. Output dir is `outputs/fleurs_tamil_eval_pretrained_0_6b`. Validation passed: `bash -n` and full FLEURS dry-run found 591 samples, zero missing audio, and zero empty references. Initial jobs `168760` and `168761` were cancelled while tuning resources; final job `168762` used 4 CPUs and 16G memory, ran on `smc-pod-4`, and completed with exit code `0:0` in 3m54s. It processed 591/591 samples. Baseline metrics: WER `1.34375640762764`, CER `1.0193030406559618`, reference words `9754`, prediction words `12958`, reference chars without spaces `78647`, prediction chars without spaces `79148`.
- T4 complete. Final Tamil SFT job `168749` completed with exit code `0:0` in 1h19m40s on `smc-pod-4`. Final checkpoint is `outputs/qwen3-asr-0.6b-tamil-sft/checkpoint-2417`; final training summary reports `train_loss=1.3200707166314274`, `train_runtime=4768.3891`, and final observed eval loss `0.05656847730278969`.
- T5 complete. Tamil fine-tuned eval job `168781` loaded `checkpoint-2417`, ran on `smc-pod-4`, completed with exit code `0:0` in 6m46s, and processed 591/591 FLEURS Tamil samples. Output dir is `outputs/fleurs_tamil_eval`. Metrics: WER `0.45171211810539263`, CER `0.16112060129825762`, reference words `9754`, prediction words `10012`, reference chars without spaces `78647`, prediction chars without spaces `79365`.
